# =============================================================================
# TraceRAG 服务端配置
# 复制为 .env 并填入实际值；.env 已被 .gitignore 忽略。
# =============================================================================

# -----------------------------------------------------------------------------
# LLM 生成模型（OpenAI 兼容接口）
# -----------------------------------------------------------------------------
DEEPSEEK_API_KEY=your_api_key_here
RAG_BASE_URL=https://api.deepseek.com
RAG_MODEL=deepseek-chat

# -----------------------------------------------------------------------------
# 数据与索引路径（相对路径以项目根目录为基准）
# -----------------------------------------------------------------------------
RAG_DATA_DIR=./data
RAG_VECTORSTORE_DIR=./vectorstore
RAG_CACHE_DIR=./.rag_cache
RAG_LOG_DIR=./logs

# -----------------------------------------------------------------------------
# 分片参数（修改后需要重建向量库）
# -----------------------------------------------------------------------------
RAG_CHUNK_SIZE=900
RAG_CHUNK_OVERLAP=120
RAG_VECTORSTORE_BATCH_SIZE=128

# -----------------------------------------------------------------------------
# 服务监听
# - 公网部署：保持 127.0.0.1，由 cloudflared/Nginx 反代暴露
# - 本机调试：可改为 0.0.0.0 临时让局域网访问
# -----------------------------------------------------------------------------
RAG_HOST=127.0.0.1
RAG_PORT=8000

# -----------------------------------------------------------------------------
# 应用层 API Key（必填，公网部署强烈建议设置）
# 客户端请求时必须携带 Authorization: Bearer <RAG_API_KEY>
# 生成方法：python -c "import secrets; print(secrets.token_urlsafe(32))"
# 留空则跳过认证（仅用于本机开发）
# -----------------------------------------------------------------------------
RAG_API_KEY=

# -----------------------------------------------------------------------------
# Cloudflare Tunnel（公网部署时使用）
# - RAG_PUBLIC_URL 是 cloudflared 路由到的对外域名
# - 留空则不启用隧道说明，仅作为客户端配置参考
# -----------------------------------------------------------------------------
RAG_PUBLIC_URL=
