{
  "name": "smillick-org-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.33.1"
  }
}
